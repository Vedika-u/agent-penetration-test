OLLAMA_MODEL=llama3.2
OLLAMA_BASE_URL=http://localhost:11434
MEMORY_DB_PATH=data/memory.sqlite3
SEARCH_FIXTURES_PATH=fixtures/search_fixtures.json
API_PORT=8000
# Leave empty (default) for local use / the harness -- see config.py. Only set this if exposing
# /chat and /attack beyond localhost.
API_AUTH_TOKEN=
